import { BaseController } from 'mct-common';
import { CrossbarDTO } from '../dtos/CrossbarDTO';
import { MountainDTO } from '../dtos/MountainDTO';

export class CrossbarController extends BaseController<CrossbarDTO>{
    
}